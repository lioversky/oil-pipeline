{
  "processors": [
    {
      "processorType": "select_remove",
      "params": {
        "fields": "a,b"
      }
    },
    {
      "processorType": "select_keep",
      "params": {
        "fields": "a,b"
      }
    },
    {
      "processorType": "select_removenull",
      "params": {
        "fields": "a,b"
      }
    }
  ]
}